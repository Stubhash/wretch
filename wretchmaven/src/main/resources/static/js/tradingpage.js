var app = angular.module("setPoints", []);
app.controller("points", function($http, $scope) {
	var controller = this;

	

	controller.updatePoints = [];
	$scope.updatePoints = function() {
		if(One=ar points1= $scope.One;
		
		Userpoints
		};

		$http.post("/rating", Userpoints);
	}
});